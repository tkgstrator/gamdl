services:
  gamdl:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        GAMDL_VERSION: 3.7.3
        PYTHON_VERSION: 3.14
        BASE_IMAGE: bookworm-slim
    tty: true
    stdin_open: true